import React from 'react';

//Style
import classes from "./header.module.css"

export const Header = () => {
    return (
        <div className={classes.header_wrp}>
            Its Header
        </div>
    );
};